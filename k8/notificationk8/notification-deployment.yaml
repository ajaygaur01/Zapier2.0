apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification
  template:
    metadata:
      labels:
        app: notification
    spec:
      containers:
      - name: notification
        image: ajaygaur0103/notification-service:latest
        envFrom:
        - configMapRef:
            name: notification-config
        ports:
        - containerPort: 9000
