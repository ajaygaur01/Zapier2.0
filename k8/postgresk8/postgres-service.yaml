apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "5432"
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
