apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: kafka-pool
  namespace: zapier
  labels:
    strimzi.io/cluster: zapier-kafka

spec:
  replicas: 1

  roles:
    - broker
    - controller

  storage:
    type: ephemeral